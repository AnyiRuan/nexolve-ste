# Nexolve Site
Next.js (App Router) + Tailwind website for implementation-focused services.

## Quick Start
```bash
npm install
npm run dev
# open http://localhost:3000
```
Deploy on Vercel. Configure API/CRM integration in `/app/api/lead/route.js`.
