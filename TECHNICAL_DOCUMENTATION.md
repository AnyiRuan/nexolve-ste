# Nexolve Website - Technical Documentation

## 项目概述

Nexolve 官网是一个基于 Next.js 14 的现代化多语言企业网站，展示公司的技术服务和解决方案。

### 技术栈
- **框架**: Next.js 14.2.5 (App Router)
- **UI 框架**: React 18.2.0
- **样式**: Tailwind CSS 3.4.3
- **国际化**: next-intl 4.5.2
- **部署**: Vercel
- **版本控制**: Git/GitHub

---

## 项目结构

```
nexolve-ste/
├── app/                          # Next.js App Router 目录
│   ├── [locale]/                 # 多语言路由
│   │   ├── about/               # 关于我们页面
│   │   ├── contact/             # 联系页面
│   │   ├── impact/              # 影响力页面
│   │   ├── legal/               # 法律文档
│   │   ├── playbooks/           # 工作手册
│   │   ├── services/            # 服务页面
│   │   │   ├── ai-automation/
│   │   │   ├── blockchain-compliance/
│   │   │   ├── custom-apps/
│   │   │   ├── digital-marketing/
│   │   │   ├── website-digital-growth/
│   │   │   └── wechat-mini-program/
│   │   ├── stories/             # 成功案例
│   │   ├── use-cases/           # 使用场景
│   │   ├── layout.jsx           # 多语言布局
│   │   └── page.jsx             # 首页
│   ├── api/                      # API 路由
│   │   └── lead/                # 潜在客户表单处理
│   ├── layout.jsx                # 根布局
│   └── page.jsx                  # 根页面（重定向）
├── components/                   # React 组件
│   ├── CTASection.jsx           # 行动号召区域
│   ├── Hero.jsx                 # 首页英雄区域
│   ├── LanguageSwitcher.jsx     # 语言切换器
│   ├── ServiceCards.jsx         # 服务卡片
│   ├── Testimonial.jsx          # 客户评价
│   └── ValueGrid.jsx            # 价值主张网格
├── i18n/                         # 国际化配置
│   ├── request.js               # 请求配置
│   └── routing.js               # 路由配置
├── messages/                     # 翻译文件
│   ├── en.json                  # 英文
│   ├── es.json                  # 西班牙文
│   └── zh.json                  # 中文
├── public/                       # 静态资源
├── .gitignore                    # Git 忽略文件
├── middleware.js                 # Next.js 中间件（语言检测）
├── next.config.js                # Next.js 配置
├── package.json                  # 项目依赖
├── postcss.config.js             # PostCSS 配置
├── tailwind.config.js            # Tailwind CSS 配置
└── README.md                     # 项目说明
```

---

## 核心配置说明

### 1. Next.js 配置 (`next.config.js`)

```javascript
const withNextIntl = require('next-intl/plugin')();

const nextConfig = {
  // App Router 在 Next.js 14 中已稳定，无需实验性标志
};

module.exports = withNextIntl(nextConfig);
```

**重要说明**:
- ✅ 已移除过时的 `experimental.appDir` 配置
- 使用 `next-intl/plugin` 包装配置以支持国际化
- App Router 是默认启用的，无需额外配置

### 2. 国际化配置

#### 路由配置 (`i18n/routing.js`)
定义支持的语言和默认语言：
- 支持语言: `['en', 'zh', 'es']`
- 默认语言: `'en'`
- URL 模式: `/[locale]/path`

#### 中间件 (`middleware.js`)
自动检测用户语言偏好并重定向到相应的语言版本。

#### 翻译文件 (`messages/`)
- `en.json`: 英文翻译（主要语言）
- `zh.json`: 简体中文翻译
- `es.json`: 西班牙语翻译

### 3. 样式系统

使用 Tailwind CSS 3.4.3，配置文件为 `tailwind.config.js`。

**自定义类和装饰**:
- `.card` - 卡片样式
- `.btn` - 按钮基础样式
- `.btn-primary` - 主要按钮
- `.btn-secondary` - 次要按钮
- `.pattern-dots` - 点状图案背景
- `.pattern-grid` - 网格图案背景
- `.decorative-blob` - 装饰性色块
- `.decorative-line` - 装饰性线条
- 颜色变量: `brand`（品牌色）、`accent`（强调色）

---

## 首页内容架构

首页由以下组件组成（按顺序）:

### 1. Hero Section (`components/Hero.jsx`)
- **标题**: "Implementation. Automation. Results."
- **副标题**: 公司介绍（约 165 词）
- **CTA 按钮**: "Get a Quote" 和 "Explore Stories"
- **设计特点**:
  - 渐变背景
  - 装饰性网格图案
  - 装饰性色块（左上和右下）
  - 几何形状装饰
  - 动画效果（淡入、滑入）

### 2. Value Grid (`components/ValueGrid.jsx`)
四个核心价值主张卡片：

| 卡片 | 图标 | 标题 | 描述 |
|------|------|------|------|
| 1 | ⚡ | Intelligent Automation Systems | 智能自动化、SaaS 平台、移动应用 |
| 2 | 🤖 | Deep Technical Expertise | AI 代理、机器学习、云原生、区块链 |
| 3 | 📊 | Vision to Innovation | 愿景到产品，可量化成果 |
| 4 | 🔒 | Technology Powers Growth | 技术驱动企业增长 |

**设计特点**:
- 每个卡片有独特的图标和颜色主题
- 装饰性背景图案
- 悬停动画效果
- 渐变色块和竖线装饰

### 3. Service Cards (`components/ServiceCards.jsx`)
展示 6 大服务：
1. AI & Automation Implementation
2. Website & Digital Growth
3. Custom App Development
4. Blockchain & Compliance
5. Digital Marketing
6. WeChat Mini Program

### 4. Testimonial (`components/Testimonial.jsx`)
客户评价区域

### 5. CTA Section (`components/CTASection.jsx`)
最终行动号召

---

## 服务页面详情

### Website & Digital Growth 服务

**文件位置**: `app/[locale]/services/website-digital-growth/page.jsx`

**核心内容**:
- Next.js 网站 + 集成 CMS
- SEO + Core Web Vitals 优化
- 分析和营销自动化
- **AI 智能聊天机器人解决方案**:
  - Custom Customer Service Chatbot（客服机器人）
  - Sales Intelligence Chatbot（销售机器人）
  - Technical Support Chatbot（技术支持机器人）
  - HR Assistant Chatbot（人力资源机器人）
- 持续维护和更新

**价格**: $5k–$10k + $800–$2k/月

---

## 内容更新指南

### 更新首页介绍

编辑 `messages/en.json`:

```json
{
  "hero": {
    "title": "主标题",
    "subtitle": "副标题描述（推荐 150-200 词）",
    "ctaPrimary": "主按钮文字",
    "ctaSecondary": "次按钮文字"
  },
  "valueGrid": {
    "implementationFirst": {
      "title": "卡片1标题",
      "text": "卡片1描述"
    },
    // ... 其他卡片
  }
}
```

**同步更新其他语言**:
- 中文: `messages/zh.json`
- 西班牙语: `messages/es.json`

### 添加新服务

1. 在 `app/[locale]/services/` 创建新文件夹
2. 创建 `page.jsx` 文件
3. 更新 `messages/*.json` 中的服务信息
4. 更新 `components/ServiceCards.jsx` 添加卡片

### 修改样式

编辑 `tailwind.config.js` 或在 `app/globals.css` 中添加自定义样式。

---

## 部署流程

### 自动部署（推荐）

项目已连接到 Vercel，任何推送到 `main` 分支的更改都会自动部署。

```bash
# 1. 进行更改
git add .
git commit -m "描述更改"
git push origin main

# 2. Vercel 自动检测并部署
# 3. 访问 https://nexolve-ste.vercel.app 查看更新
```

### 本地构建测试

```bash
# 安装依赖
npm install

# 开发模式
npm run dev

# 生产构建
npm run build

# 启动生产服务器
npm start
```

---

## 重要更新记录

### 2025-12-12
1. **移除过时配置**
   - 删除 `experimental.appDir` 配置（Next.js 14 中已废弃）

2. **优化 .gitignore**
   - 添加 `.next/`、`node_modules/` 等构建文件
   - 标准化 Next.js 项目忽略规则

3. **添加 AI 聊天机器人服务**
   - 在 Website & Digital Growth 服务中添加 4 种聊天机器人
   - 使用专业英文描述

4. **优化首页内容**
   - 更新 Hero 副标题为更全面的公司介绍
   - 将 "company" 改为 "firm" 提升专业性
   - 重组 Value Grid 为 4 个新的价值主张
   - 添加可量化的成果数据

5. **UI 增强**
   - 为 Hero 添加装饰性元素（渐变背景、网格图案、色块）
   - 为 Value Grid 添加图标、颜色主题和动画
   - 改进整体视觉层次和用户体验

---

## 开发最佳实践

### 代码规范
1. 组件使用 JSX 格式
2. 使用 `'use client'` 标记客户端组件
3. 国际化文本使用 `useTranslations` hook
4. 保持组件小而专注

### 性能优化
1. 使用 Next.js 的静态生成（SSG）
2. 图片使用 Next.js Image 组件
3. 代码分割和懒加载
4. 优化 Tailwind CSS（PurgeCSS）

### SEO
1. 每个页面设置 `metadata`
2. 使用语义化 HTML
3. 优化 Core Web Vitals
4. 多语言 SEO（hreflang）

---

## 故障排查

### 常见问题

**1. 构建错误: "Cannot find module 'next-intl/plugin'"**
```bash
npm install
```

**2. 翻译未显示**
- 检查 JSON 文件语法
- 确认翻译 key 与代码中使用的一致
- 重启开发服务器

**3. 样式未生效**
- 检查 Tailwind 配置
- 确认类名正确
- 清除 `.next` 缓存并重新构建

**4. 部署失败**
- 检查 Vercel 构建日志
- 确认 `package.json` 中的依赖版本
- 验证环境变量配置

---

## 联系与支持

**项目仓库**: https://github.com/AnyiRuan/nexolve-ste
**部署地址**: https://nexolve-ste.vercel.app

---

## 未来升级建议

### 短期（1-3 个月）
- [ ] 添加博客功能
- [ ] 集成分析工具（Google Analytics）
- [ ] 添加更多案例研究
- [ ] 优化移动端体验

### 中期（3-6 个月）
- [ ] 实现在线聊天功能
- [ ] 添加客户门户
- [ ] 集成 CMS（如 Sanity 或 Contentful）
- [ ] A/B 测试框架

### 长期（6-12 个月）
- [ ] 升级到 Next.js 15（发布后）
- [ ] 添加更多语言支持
- [ ] 实现个性化内容推荐
- [ ] 完整的客户自助服务平台

---

*最后更新: 2025-12-12*
*文档版本: 1.0*
